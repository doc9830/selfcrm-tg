node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
# Секреты локального запуска Worker (`npx wrangler dev`) — как .env, в git не попадают.
.dev.vars
.DS_Store
.idea
.vscode/*
!.vscode/extensions.json
.capacitor
promt.md
# Техзадание с данными бота (живой токен) — в репозиторий не попадает.
SelfCRM — Telegram Release System.md

# Техническое задание (PROMPT.md) и локальные настройки не публикуются.
PROMPT.md
